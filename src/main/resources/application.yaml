spring:
  application:
    name: book_finder

  datasource:
    url: jdbc:postgresql://localhost:5432/bf_database
    username: ${DB_USERNAME:author}
    password: ${DB_PASSWORD:Q9vR8k2pLsT4}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
